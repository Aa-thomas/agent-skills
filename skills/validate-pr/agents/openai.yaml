interface:
  display_name: "Validate PR"
  short_description: "Assess pull request readiness and evidence"
  default_prompt: "Use $validate-pr to assess whether the current branch is ready for a pull request."

policy:
  allow_implicit_invocation: true
